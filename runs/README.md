# Runs

One directory per run, each containing `run.json`.

**Every number in the paper is generated from these files.** Nothing is typed by
hand into a table. If a number appears in the manuscript and cannot be traced to
a `run.json` here, it is a bug.

Model weights are gitignored — archive them to a Kaggle Dataset instead.
